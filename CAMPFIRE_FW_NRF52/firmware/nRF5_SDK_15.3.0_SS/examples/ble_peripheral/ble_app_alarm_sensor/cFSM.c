#include <stdio.h>
#include "cFSM.h"
#include "nrf_log.h"


int fsm_main_loop()
{
//	state_fun = state[cur_state];
//	rc = state_fun();
//	if (EXIT_STATE == cur_state)
//	{
//			return cur_state;
//	}
//	cur_state = lookup_transitions(cur_state, rc, state_transitions, sizeof(state_transitions)/sizeof(state_transitions[0]));
}
